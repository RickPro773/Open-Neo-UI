# Open Neo UI Docker

Infraestrutura Docker da nova arquitetura.

Versao: `0.3.3-beta`

## Servicos planejados

- `api-python`: FastAPI local;
- `ollama`: opcional, se o usuario nao usar Ollama nativo;
- `ollama`: modelos locais.

Nao ha infraestrutura remota obrigatoria na arquitetura principal.
