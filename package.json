{
  "name": "open-neo-ui",
  "version": "0.2.1-alpha.2",
  "license": "ISC",
  "description": "Local AI proxy — OpenAI-compatible, multi-provider, tool execution, system tray",
  "scripts": {
    "dev":     "bun --watch src/index.ts",
    "start":   "bun run src/index.ts",
    "test":    "bun test",
    "build":   "bun run scripts/build.ts",
    "py":      "cd py && python bridge.py"
  },
  "dependencies": {
    "hono":   "^4.4.0",
    "jose":   "^5.3.0",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
